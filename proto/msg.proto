syntax = "proto3";

option go_package = "/proto";
package proto;

// FIXME и что же ты разворачиваешь? 
service Reverse {

// FIXME: названия на 10 из 10.
  rpc Do(SendNotifier) returns (ResponseNotifier) {}
}

message SendNotifier {
  string TypeMessage = 1;
  string Value = 2;
  string Frequency = 3;
}

message ResponseNotifier {
  string message = 1;
}